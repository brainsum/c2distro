langcode: en
status: true
dependencies:
  config:
    - field.storage.user.field_password_never_expires
  module:
    - user
  enforced:
    module:
      - password_policy
id: user.user.field_password_never_expires
field_name: field_password_never_expires
entity_type: user
bundle: user
label: 'Password never expires'
description: 'If it is checked the password will never expires for this user.'
required: false
translatable: false
default_value:
  -
    value: 0
default_value_callback: ''
settings:
  on_label: 'On'
  off_label: 'Off'
field_type: boolean
