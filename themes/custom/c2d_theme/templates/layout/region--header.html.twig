{#
/**
 * @file
 * Theme override to display a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region <div>.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 *
 * @see template_preprocess_region()
 */
#}
{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
  ]
%}

{% if content %}
  <div{{ attributes.addClass(classes) }}>
    {{ content }}

    {{ site_name }}

    <div class="social-block">
      <a href="{{ url('view.search.page') }}" target="_blank" rel="nofollow" class="search-icon">
        <img src="/{{ c2d_theme_path }}/images/dist/icons/IFRC_icn_search.svg" alt="{{ 'magnifier icon'|t }}">
      </a>
    </div>
    <div class="mobile-icons">
      <div class="hamburger">
        <img src="/{{ c2d_theme_path }}/images/dist/icons/IFRC_icn_hamburger.svg" alt="{{ 'menu icon (three stripes)'|t }}">
      </div>
    </div>
    <a class="button-link-container" href="/donate" aria-label="Donate">
      <span id="" class="button donate-button">
        {{ "Donate"|t }}
      </span>
    </a>
  </div>
{% endif %}
